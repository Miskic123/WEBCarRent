class Comment {
  constructor(id, buyer, rentalObject, text, grade) {
    this.id = id;
    this.buyer = buyer;
    this.rentalObject = rentalObject;
    this.text = text;
    this.grade = grade;
  }
}

module.exports = Comment;
